class TesterForme extends Program{

    NoeudForme newNoeudForme(Coodonnee[] coords){
        NoeudForme formes = new NoeudForme();
        for( int i = 0; i < length(coords); i++){
            //
        }
    }

    void algorithm(){
        NoeudForme formes = newNoeudForme(int taille);
    }
}
